In questo file, che useremo come blocco note, ci appunteremo tutti i piccoli problemi o warning che vediamo e dovremo risolverli tutti. L'obiettivo è finire il progetto in modo che non lanci nessun warning, o comunque che non avvengano errori. 

ULTIME FUNZIONALITA' PRINCIPALI:

  - Sistema di notifiche per messaggi non letti
  - Sistemare la chat graficamente, sia in gioco che nella home (e anche agli spettatori ma tanto quella è uguale a quella di gioco)
  - Pannello amministratore: bloccare utente; bannare utente; promuovere utente ad admin; vedere statistiche; resettare statistiche; inviare avvisi a tutti gli utenti
    (sotto forma di notifica); viste da db con diversi ordinamenti (in base al punteggio o altre statistiche). Pensavamo di salvarci nel client tutti gli utenti
    e ordinarli direttamente da lì senza fare query complicate ogni volta; per ordinarli basta cliccare sul nome della colonna che contiene il valore del quale
    ci interessa l'ordinamento.
  - Se clicchi su un utente vedi in un pop up le sue statistiche
  - App mobile

IMPLEMENTAZIONI E FIX:
  0. Le partite non vengono vinte correttamente; se A lascia, B vince due volte, poi se si fa un'altra partita e B lascia, A vince 3 volte (secondo
     me è un problema di qualche socket che rimane attivo)
  1. Segnalare con un errore che il login non è andato a buon fine
  2. Allungare le sessioni di login
  3. Eliminare bene le notifiche senza lasciare null (quando si accetta un invit0)
  4. Quando blocchi un utente si deve anche eliminare dalla friends list
  5. Quando inizia una partita chiudere tutti i menu a tendina
  6. Sistemare il friend component che fa delle emit inutili; praticamente quando bisogna eliminare una friend request dalla lista, 
     si fa fare la POST al friend-request-service, poi il server ritorna la nuova blacklist, poi il service sente la response, la carica
     in localstorage, fa una emit per dire al server di dire al friend component di aggiornare la sua blacklist...
     Quando basterebbe che dal component, appena si gestisce una request, la si elimini dalla lista direttamente dal component senza fare mille
     emit per niente
  7. Chat component fisso in absolute in cima a tutto (anche nella home e in my profile)
  8. Migliorare graficamente lo spectator component html
  9. Aggiungere lista degli utenti bloccati da poter sbloccare
  10. Controllare TUTTE le volte che viene salvato il current user in localstorage e togliergli salt digest e password mettendoli a null
      prima di fare la localStorage.setItem('current user')
  11. Mettere commenti in inglese ovunque
  12. Eliminare file inutili

